#!/bin/bash
# Deploy to GCS (ARCHIVED)

# This script has been archived as the project now uses Firebase Hosting
# instead of Google Cloud Storage for static website hosting.

echo "This script has been archived."
echo "The project now uses Firebase Hosting for deployment."
echo "See .github/workflows/firebase-hosting-merge.yml for current deployment process."

# Original purpose: Deploy React build to GCS bucket
# Current implementation: Firebase Hosting with GitHub Actions