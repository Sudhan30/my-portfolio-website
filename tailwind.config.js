/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/**/*.{js,jsx,ts,tsx}",
    ],
    theme: {
        extend: {
            colors: {
                // You can extend colors here if needed, keeping empty to use defaults
            },
        },
    },
    plugins: [],
}
